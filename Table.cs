namespace SDP_ASG;

public class Table : Furniture
{
    public Table(Brand brand, string type, string colour, string material, int height, int width, int depth, double price)
    : base(brand, type, colour, material, height, width, depth, price)
    { }
}
